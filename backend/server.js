require("dotenv").config();const express=require("express"),cors=require("cors"),mongoose=require("mongoose");const projectRoutes=require("./routes/projectRoutes"),messageRoutes=require("./routes/messageRoutes");const app=express();app.use(cors());app.use(express.json());app.get("/",(req,res)=>res.json({message:"Nahim Portfolio API is running"}));app.use("/api/projects",projectRoutes);app.use("/api/messages",messageRoutes);const PORT=process.env.PORT||5000;mongoose.connect(process.env.MONGO_URI||"mongodb://127.0.0.1:27017/nahim_portfolio").then(()=>app.listen(PORT,()=>console.log(`API running on http://localhost:${PORT}`))).catch(e=>{console.error("MongoDB connection failed:",e.message);app.listen(PORT,()=>console.log(`API running without MongoDB on ${PORT}`))});